pre-commit:
  commands:
    lint:
      run: ./node_modules/.bin/biome check --write --unsafe --no-errors-on-unmatched {staged_files}
      stage_fixed: true
      glob: "*.{ts,js,json}"
    type-check:
      run: ./node_modules/.bin/tsgo --noEmit
